import { AppLayout } from "@/layouts/app-layout";

export default function ApplicationSetting() {

    return (
        <div>
            
        </div>
    );

}

ApplicationSetting.layout = (page: any) => <AppLayout children={page} />;