import { AppLayout } from "@/layouts/app-layout";

export default function ProcedureIndex() {
    return (
        <div></div>
    )
}

ProcedureIndex.layout = (page: any) => <AppLayout children={page} />;