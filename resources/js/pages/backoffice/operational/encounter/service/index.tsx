import { AppLayout } from "@/layouts/app-layout";

export default function ServiceIndex() {
    return (
        <div></div>
    )
}

ServiceIndex.layout = (page: any) => <AppLayout children={page} />;