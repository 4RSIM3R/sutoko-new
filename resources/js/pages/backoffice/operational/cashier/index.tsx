import { AppLayout } from "@/layouts/app-layout";

export default function CashierIndex() {
    
    return (
        <div></div>
    )

}

CashierIndex.layout = (page: any) => <AppLayout children={page} />;