import { AppLayout } from "@/layouts/app-layout";

export default function CashierForm() {
    
    return (
        <div></div>
    )

}

CashierForm.layout = (page: any) => <AppLayout children={page} />;